get:
  description: 'Returns: NOT-FOUND,DELETED,UPDATED,APPROVED'
  parameters: []
  responses:
    '200':
      description: Status 200
      schema:
        properties:
          status:
            type: string
        required:
          - status
        type: object
  summary: Returns status of a users resume
  tags:
    - resumes
x-restlet:
  section: RESUMES
