get:
  parameters:
    - in: query
      name: carrier_id
      required: false
      type: integer
  responses:
    '200':
      description: Status 200
      schema:
        properties:
          carrier_id:
            type: integer
          carrier_name:
            type: string
        required:
          - carrier_id
          - carrier_name
        type: object
  summary: Get a list of mobile_carriers.
  tags:
    - profiles
x-restlet:
  section: PROFILES
