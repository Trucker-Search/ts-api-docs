properties:
  list:
    description: conversation in chronological order
    items:
      $ref: '#/definitions/conversation_list_list'
    type: array
required:
  - list
type: object
x-restlet:
  section: MESSAGES
