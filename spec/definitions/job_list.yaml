properties:
  display_columns:
    items:
      description: list of columns and order to be given display preference.
      example: ''
      type: string
    type: array
  list:
    items:
      $ref: '#/definitions/job_list_list'
    type: array
  page_number:
    type: integer
  total_items:
    type: integer
  total_pages:
    type: integer
required:
  - display_columns
  - list
  - page_number
  - total_items
  - total_pages
type: object
x-restlet:
  section: JOBS
