properties:
  email:
    type: string
  password:
    type: string
  status:
    description: It defaults to employer.
    enum:
      - employer
      - driver
    type: string
required:
  - email
  - password
type: object
